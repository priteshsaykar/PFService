   server:
     port: 9100
 
   spring:
     application:
       name: FileUploadMS
     cloud:
       consul:
         host: localhost
         port: 8500
         discovery:
           enabled: true
         config:
           enabled: true
           prefixes: config
           defaultContext: application
           profileSeparator: '::'
           format: YAML
         